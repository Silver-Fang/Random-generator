<Page
    x:Class="随机生成器2.数值"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:随机生成器2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Page.Resources>
		<Flyout x:Name="错误提示">
			<TextBlock x:Name="错误消息"/>
		</Flyout>
	</Page.Resources>
	<StackPanel>
		<StackPanel Orientation="Horizontal">
			<TextBox x:Name="最小值" Header="最小值"/>
			<TextBox x:Name="最大值" Header="最大值"/>
		</StackPanel>
		<CheckBox x:Name="浮点" Content="浮点"/>
		<StackPanel Orientation="Horizontal">
			<CheckBox x:Name="规定平均值" Content="平均值"/>
			<TextBox x:Name="平均值" IsEnabled="{Binding IsChecked, ElementName=规定平均值}"/>
		</StackPanel>
		<StackPanel Orientation="Horizontal">
			<CheckBox x:Name="规定标准差" Content="标准差"/>
			<TextBox x:Name="标准差" IsEnabled="{Binding IsChecked, ElementName=规定标准差}"/>
		</StackPanel>
		<StackPanel Orientation="Horizontal">
			<TextBox x:Name="数值_生成个数" Header="生成个数"/>
			<CheckBox x:Name="数值_无重复" Content="无重复"/>
		</StackPanel>
		<Button x:Name="数值_生成" Content="生成"/>
		<TextBlock IsTextSelectionEnabled="True"/>
	</StackPanel>
</Page>
